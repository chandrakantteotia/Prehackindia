{
  "name": "sharpplay-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for SharpPlay Web3 Gaming Platform",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:mumbai": "hardhat run scripts/deploy-token.js --network mumbai",
    "deploy:polygon": "hardhat run scripts/deploy-token.js --network polygon",
    "verify": "hardhat verify --network polygon",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
